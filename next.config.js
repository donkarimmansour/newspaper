/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn.betterstudio.com' , "demo.betterstudio.com"],
  },
}

module.exports = nextConfig
